/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Statics : MonoBehaviour
{
    // with static variables each object in the class has the same variable with the same value
    // this means changing the value of a static variable in one place will result in a change of value in all of the others.


    // EX: if we want to know how many objects of an enemy class have been instantiated
    
    public static int enemyCount = 0;

    public static int playerCount = 0;

    public Enemy()
    {
       enemyCount++;
    }

    void Start()
    {
        playerCount ++;
    }

    // Static means you can't create instances of that variable or class

}
*/
