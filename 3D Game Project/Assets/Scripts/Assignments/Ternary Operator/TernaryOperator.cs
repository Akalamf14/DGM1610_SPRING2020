/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TernaryOperator : MonoBehaviour
{
    // A Ternary Operator is a condensed version for an IF ELSE statement
    // is meant to choose between 2 values based on some boolean expression

    // Start is called before the first frame update
    void Start()
    {

        //syntax is -- boolean ? True expression : False expression
        int health = 10;
        string message;

        message = health > 0 ? "Player is Alive" : "Player is Dead";
        
        // the first parameter is the boolean, or condition which evaluates to a boolean that you wish to test
        //the end of this parameter is marked with a question mark

        //we can use this to determine if a player is alive or dead
        //we can nest Ternary Operators within each other, but should not be used for long expressions because it can become cumbersome and hard to read
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
*/