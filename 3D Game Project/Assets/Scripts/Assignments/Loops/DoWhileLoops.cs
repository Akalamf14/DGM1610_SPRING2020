/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DoWhileLoops : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // do while are almost the same as while loops
        //the only difference is while loops test the condition before the loop body
        //do while test the conditions at the end of the body
        // its guaranteed to run at least once


        bool shouldContinue = false;

        do
        {
            print("What's Up?");
        }
        while (shouldContinue == true); //(shouldContinue == true) is the conditional
        
        //the loop will only continue WHILE the variable (shouldContinue) is true
        //the variable is set to false which means once the loop is completed one time it will automatically switch to false and exit the loop
        //because the conditions are tested after loop was run once
        //REMEMBER DOWHILE LOOPS ALWAYS RUN AT LEAST ONCE
        // while loops DON'T have semi colons, BUT doWHile loops do have the semi colon

  }


    // Update is called once per frame
    void Update()
    {
        
    }
}
*/
