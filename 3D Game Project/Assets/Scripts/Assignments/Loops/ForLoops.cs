/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ForLoops : MonoBehaviour
{

    //most common loop 
    // works by creating a loop with a controlable number of iterations
    // first checks for conditions

    //loop is also known as iteration
    int numEnemies = 3;

    // Start is called before the first frame update
    void Start()
    {
      for(int i = 0; i < numEnemies; i++)
      {
          Debug.Log("Creating Enemy number" + i);
      }  
      //there are three arguments
      // int i = 0 is an interator - used to count through the iterations of the loops or the loops themselves
      // i < numEnemies - condition that must be true for the loop to continue
      // i ++ - determines what happens to the iterator in each loop
        // the loop will continue until i is no longer less than the numEnemies which in this case is 3
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
*/
